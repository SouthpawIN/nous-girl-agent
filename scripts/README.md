// scripts/ — install, run, dev scripts

- `install.sh` (TBD) — one-shot setup: install uv deps, copy default config
- `run-VA.sh` (TBD) — start the VA (wraps vtuber-core)
- `run-radio.sh` (TBD) — start the radio plugin
- `run-agent.sh` (TBD) — start the curator agent
- `dev.sh` (TBD) — full dev environment (all three + tail logs)
